<div class="container">
  <h1>Your pets</h1>

  <ul>
    <% @pets.each do |pet| %>
    <li>
      <%= link_to pet.name, pet_path(pet) %> (<%= pet.color %> <%= pet.species %>)
      - found <%= pluralize pet.found_days_ago, 'day' %> ago
    </li>
    <% end %>
  </ul>
</div>

<h1>Your pets!</h1>

<ul>
  <% @pets.each do |pet| %>
    <li>
      <%= link_to pet.name, pet_path(pet) %> (<%= pet.color %> <%= pet.species %>)
      - found <%= pet.found_days_ago %> days ago
    </li>
  <% end %>
</ul>
